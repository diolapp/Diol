syntax = "proto2";

option java_package = "app.diol.dialer.callintent";
option java_multiple_files = true;
option optimize_for = LITE_RUNTIME;


package app.diol.dialer.callintent;

// The type of speed call contact (favorites). Applies to initiation type
// SPEED_DIAL only.
message SpeedDialContactType {
    enum Type {
        UNDEFINED = 0;
        PINNED_CONTACT = 1;
        STARRED_CONTACT = 2;
        FREQUENT_CONTACT = 3;
    }
}
